// Helper to go to the next step
const goNext = () => cy.findByText('Next').click()

describe('User Registration', () => {
  beforeEach(() => {})

  it('Visits the page', () => {})

  it('Fill in the form', () => {})

  it('Submit the form', () => {})
})
